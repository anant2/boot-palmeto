server: 
  port : 9000

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springdb?useSSL=false
    username: root
    password: root
    
    logging:
        level:
          org:
           springframework:
              security: DEBUG
    